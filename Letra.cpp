#include <ctjpe.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>


int main()

{char letra, converte;
	
	printf ("Digite um caracter: ");
	scanf("%c",&letra);

		if (isalpha(letra))
			if (islower)
		 		if (isupper)
			
			converte=tolower(letra);
			converte=toupper(letra);
	{
		printf ("\nvoce digitou uma letra: %c \n\n", letra);
		
	}






}
